rrhpeco(){
    data=$(rrh list --no-header --format blank --entries all | peco)
    if [[ $(echo $data | wc -l) -ne 1 ]]; then
        echo "multiple entries are remained"
        return 1
    fi
    cd $(echo $data | awk -F ' ' '{ print $3 }')
    pwd
}
